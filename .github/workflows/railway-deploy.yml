name: Deploy to Railway

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: railway-deploy-main
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check RAILWAY_TOKEN
        id: check
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          if [ -z "$RAILWAY_TOKEN" ]; then
            echo "::warning::RAILWAY_TOKEN secret not set in GitHub. Skipping CI deploy — Railway native GitHub integration handles deploys instead. To enable this workflow as backup, add RAILWAY_TOKEN secret in Settings → Secrets and variables → Actions."
            echo "has_token=false" >> $GITHUB_OUTPUT
          else
            echo "has_token=true" >> $GITHUB_OUTPUT
          fi

      - name: Install Railway CLI
        if: steps.check.outputs.has_token == 'true'
        run: |
          curl -fsSL https://railway.app/install.sh | sh
          echo "$HOME/.railway/bin" >> $GITHUB_PATH

      - name: Railway up (deploy current commit)
        if: steps.check.outputs.has_token == 'true'
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          railway up --ci --service ${{ vars.RAILWAY_SERVICE_NAME || 'suno-api' }}

      - name: Post-deploy health check
        if: steps.check.outputs.has_token == 'true'
        run: |
          URL="${{ vars.RAILWAY_PUBLIC_URL || 'https://suno-api-production-cfc9.up.railway.app' }}"
          echo "Polling $URL/api/health (max 5 minutes)..."
          for i in $(seq 1 30); do
            code=$(curl -s -o /tmp/health.json -w "%{http_code}" --max-time 15 "$URL/api/health" || echo "fail")
            if [ "$code" = "200" ]; then
              echo "Health check passed:"
              cat /tmp/health.json
              exit 0
            fi
            echo "Attempt $i: HTTP $code — retrying in 10s..."
            sleep 10
          done
          echo "::error::Health check failed after 5 minutes"
          exit 1
